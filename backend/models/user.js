class User {
    constructor(fullName, dob, email, password) {
      this.fullName = fullName;
      this.dob = dob;
      this.email = email;
      this.password = password;
    }
  }
  
  module.exports = User;